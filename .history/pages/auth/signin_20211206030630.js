import { getProviders, signIn } from 'next'

function signIn() {
  return (
    <div>
      
    </div>
  )
}

export async function getServerSideProps() {
  const providers = getProviders
}

export default signIn
